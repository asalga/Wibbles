var Settings = {
    fullscreen: false,
    bgColor: 0x0000AA,

    godMode: false,
    numLives: 4,
    blocksPerSecond: 16,
    foodPerLevel: 1,

    blockSize: 1,
    boardColumns: 80,
    // first two rows are for the hud/score
    boardRows: 50,

    debugOn: true
};

module.exports = Settings;